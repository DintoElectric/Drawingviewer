Drop new PDF drawings in this folder (or upload them here in the GitHub web UI).
The "Ingest drawings" Action reads each title block, renames it to <SHEET>_Rev<N>.pdf,
archives the version it supersedes, and updates public/manifest.json.
